<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment_05">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".contactus"></activity>
        <activity android:name=".aboutus" />
        <activity android:name=".findloc" />
        <activity android:name=".success" />
        <activity android:name=".doations" />
        <activity android:name=".newsandevnts" />
        <activity android:name=".problems" />
        <activity android:name=".latest_posts" />
        <activity android:name=".SignUp" />
        <activity android:name=".login_success" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>